//grab as JSON
$.getJSON("/articles", function(data){
    //run through each
    for (var i = 0; i < data.length; i++) {
        //display
        $("#articles").append()
    }
});

li.feature-list-wrapper